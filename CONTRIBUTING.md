# 開発参加ガイド（チームで一緒に育てるために）

このシステム（会計データチェッカー）は、みんなでチェック項目を増やしながら精度を高めていくことを目指しています。
このページは、**新しく開発に参加するメンバー向けの入口**です。プログラミングに詳しくなくても、Claude に頼みながら進められるように書いています。

---

## 0. 大事な前提：みんなで共有するのは「GitHub」です

- このシステムの本体は **GitHub リポジトリ `harel-zei/kaikei-checker`** です。
- Claude は各自が使う「道具」です。**メンバーはそれぞれ自分の Claude アカウント（個人契約でOK）**を使えます。
  チーム版の Claude アカウントは必要ありません。
- 全員が同じ GitHub リポジトリにつなぎ、**ブランチ → プルリクエスト（PR）→ レビュー → マージ**の流れで変更を合流させます。

```
  あなたのClaude ┐
  同僚のClaude   ┼─→  GitHub（harel-zei/kaikei-checker）  ─→  Render（自動デプロイ）
  別の同僚のClaude┘         ↑ ここが全員の共有ポイント
```

---

## 1. 参加の手順（初回だけ）

### ① GitHub アカウントを用意する
まだ持っていなければ https://github.com で無料アカウントを作成します。
作成したら、自分の **GitHub ユーザー名** を管理者（岩崎）に伝えてください。

### ② リポジトリに招待してもらう（管理者の作業）
管理者は次の操作でメンバーを追加します。
1. GitHub でリポジトリ `harel-zei/kaikei-checker` を開く
2. **Settings → Collaborators and teams → Add people**
3. メンバーの GitHub ユーザー名を入力し、権限は **Write** を選んで招待
4. メンバーに届く招待メール／通知を **Accept（承認）** してもらう

### ③ 自分の Claude を GitHub につなぐ
1. Claude（web の [claude.ai/code](https://claude.ai/code)、またはデスクトップ／スマホアプリ）にログイン
2. GitHub 連携を有効にし、`harel-zei/kaikei-checker` を選べるようにする
3. リポジトリを開いてセッションを開始すれば、準備完了です

> 参考: Claude Code on the web の使い方 → https://code.claude.com/docs/en/claude-code-on-the-web

---

## 2. 作業の進め方（毎回の流れ）

**「1つの作業 ＝ 1つのブランチ ＝ 1つの PR」** を守ると、複数人で作業してもぶつかりません。

1. **ブランチを切る**：作業ごとに新しいブランチを作ります。
   - 例：`feature/check-印紙税`、`feature/交際費-上限チェック`
   - Claude に「〇〇のチェックを追加したいので、そのためのブランチを作って」と頼めばOKです。
2. **変更する**：Claude と相談しながらコードを追加・修正します。
3. **PR（プルリクエスト）を作る**：変更内容を説明する PR を作成します（下書き＝ドラフトで作ってOK）。
4. **レビューしてもらう**：**別のメンバーが確認**してからマージするのがおすすめです。
   - レビューで「結果が変わっていないか」「意図どおりか」を見てもらいます。
5. **マージする**：問題なければ `main` にマージします。
6. **デプロイ**：`main` の内容を `deploy/render` に反映すると、Render が自動で本番を更新します。

---

## 3. 絶対に守ってほしいこと（会計事務所として重要）

- ⚠️ **顧問先の実データ（仕訳帳・試算表など個人情報）を GitHub にコミットしない。**
  - 動作確認は**合成データ（テスト用のダミー）**で行ってください。
  - `client_data/` フォルダは `.gitignore` で除外済みです。実データはそこに置けば追跡されません。
- 🔑 **API キーやパスワードをコードに書かない／コミットしない。** 環境変数（`.env`）で管理します（`.env` も除外済み）。
- 🧪 **チェックのロジックを変えたら、「チェック結果が意図どおりに変わったか」を必ず確認**してからマージしてください。

---

## 4. チェック項目を追加したいとき

新しいチェックの追加方法は、専用ガイドにまとめてあります。
コードに不慣れでも、この手順を Claude に見せながら頼めば追加できます。

👉 **[docs/新しいチェックの追加ガイド.md](docs/新しいチェックの追加ガイド.md)**

雛形（コピーして使えるテンプレート）はこちら：
👉 **[backend/checkers/_template_checker.py](backend/checkers/_template_checker.py)**

---

## 5. 困ったときは

- やり方が分からないときは、まず自分の Claude セッションで「これこれをしたい」と相談してください。
- 仕様や方針の相談・タスクの共有は、GitHub の **Issue**（課題）に書くとチーム全員で見られます。
- それでも迷うときは管理者（岩崎）に声をかけてください。
